#defaults
spring.cloud.stream.kafka.streams.binder:
  brokers: broker1, broker2, broker3
  configuration:
   processing.guarantee: exactly_once
   commit.interval.ms: 1000
   default.key.serde: org.apache.kafka.common.serialization.Serdes$StringSerde
   default.value.serde: com.demo.streams.data.UserEventJsonSerde

#user register in
spring.cloud.stream.bindings.urin:
  destination: kafka-stream_USER
  group: group1
  consumer:
    headerMode: raw

#user register out
spring.cloud.stream.bindings.urot:
  destination: user-register-counts
  producer:
    useNativeEncoding: true
    headerMode: raw

spring.cloud.stream.kafka.streams.bindings.urot.producer:
    keySerde: org.apache.kafka.common.serialization.Serdes$StringSerde
    valueSerde: org.apache.kafka.common.serialization.Serdes$LongSerde
    dspring.json.key.default.type: java.lang.String






#logging:
## Log levels severity mapping
#  level:
#    org:
#      springframework: INFO
#      apache.kafka: DEBUG
#    kafka: DEBUG

